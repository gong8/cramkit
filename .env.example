# Database — SQLite file path (relative to monorepo root)
DATABASE_URL="file:./data/cramkit.db"

# LLM model selection (used by CLI spawning)
LLM_MODEL=claude-opus-4-6

# API server URL (used by MCP server and web frontend)
CRAMKIT_API_URL=http://localhost:8787
